services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: shillclawd
      POSTGRES_USER: shillclawd
      POSTGRES_PASSWORD: shillclawd
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
